const defaultSettingsState = {
    theme: 'ladies-night',
};

export default (state = defaultSettingsState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
